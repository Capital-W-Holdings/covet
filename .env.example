# =============================================================================
# COVET ENVIRONMENT CONFIGURATION
# =============================================================================

# Database (PostgreSQL - for production)
# Leave unset to use in-memory database (development only)
# DATABASE_URL="postgresql://user:password@localhost:5432/covet?schema=public"
# USE_PRISMA="true"

# Authentication (REQUIRED for production)
# Must be at least 32 characters for security
JWT_SECRET="your-jwt-secret-min-32-chars-here"

# Stripe Payments
# Get keys from: https://dashboard.stripe.com/apikeys
# Leave unset to use demo mode (no real payments)
# STRIPE_SECRET_KEY="sk_test_xxx"
# STRIPE_WEBHOOK_SECRET="whsec_xxx"

# Email Notifications
# Supported providers: resend, sendgrid, postmark
# Leave unset to use demo mode (logs to console)
# EMAIL_PROVIDER="resend"
# EMAIL_API_KEY="re_xxx"
# EMAIL_FROM="Covet <orders@covet.com>"

# Image Uploads
# Supported providers: s3, cloudinary, vercel-blob
# Leave unset to use demo mode (local storage)
# IMAGE_PROVIDER="s3"

# AWS S3 Configuration (if IMAGE_PROVIDER="s3")
# AWS_ACCESS_KEY_ID="AKIA..."
# AWS_SECRET_ACCESS_KEY="..."
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET="covet-images"

# Cloudinary Configuration (if IMAGE_PROVIDER="cloudinary")
# CLOUDINARY_CLOUD_NAME="your-cloud-name"
# CLOUDINARY_API_KEY="123456789"
# CLOUDINARY_API_SECRET="..."

# Vercel Blob Configuration (if IMAGE_PROVIDER="vercel-blob")
# BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# Public keys (available in browser)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_xxx"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Covet"

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
# 1. Set NODE_ENV=production
# 2. Set strong JWT_SECRET (32+ random chars)
# 3. Set DATABASE_URL to production PostgreSQL
# 4. Set USE_PRISMA=true
# 5. Set Stripe live keys (sk_live_xxx, pk_live_xxx)
# 6. Set STRIPE_WEBHOOK_SECRET from Stripe dashboard
# 7. Set EMAIL_PROVIDER and EMAIL_API_KEY
# 8. Set IMAGE_PROVIDER and storage credentials
# 9. Update NEXT_PUBLIC_APP_URL to your domain
